{
  "name": "weather_interview",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongodb": "^4.8.0"
  },
  "scripts": {
    "start": "node ./index.js",
    "build": "cd client && npm install && npm run build"
  }
}
